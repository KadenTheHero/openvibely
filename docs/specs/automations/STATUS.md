# Automation Implementation Status

## Current State

Automation Graphs is implemented as a project-scoped custom automation builder over existing OpenVibely Tasks, Scheduler rows, workers, Native Alerts, GitHub services, queues, and runtime projection. Template, Describe It, Blank, and Chat use the same surfaced graph contract and safety boundaries.

Each Automation has exactly one current saved graph. There is no Automation draft lifecycle, retained version history, superseded graph, version selection, Definition page, execution/history page, invocation page, work-item page, node-resource sidebar, Save journal, failed-Save recovery item, plan revision, or compiler contract version. Internal graph IDs exist only for current runtime identity.

## Save Contract

- New and edited web graphs exist only in browser memory until `Save changes`. Refresh or navigation discards unsaved changes.
- Save validates the complete submitted graph, applies real resources immediately, and creates or replaces the current graph under the same Automation identity. Strict normalization trims non-semantic formatting but does not overwrite `automation_type`, fill explicit empty maintained-node names, derive or overwrite Task/Schedule categories, or add custom Schedule targets from graph topology; explicit invalid values reach validation and fail before effects.
- Save may replace the current graph while Automation work is active. Successful replacement deletes the prior graph and its Automation runtime projection in the same transaction. Failure leaves the current graph and behavior unchanged.
- Template, Describe It, Blank, and Edit are browser-local until `Save changes`. `Use template` loads the maintained candidate into the builder and creates no Automation, Task, or Scheduler row.
- Maintained Native and GitHub registration is a point-in-time setup path, not an upgrade path. First registration copies that release's bundled template and complete valid maintained-node defaults into one saved graph, then overlays exact bound Task/Schedule configuration. Later registration reruns return the existing Automation, graph, bindings, lifecycle, owned Schedules, and runtime projection unchanged even when the bundled template or supplied resources differ. Trusted Edit reconstruction fills only missing canonical keys in older registered snapshots, without overwriting explicit values or mutating storage before `Save changes`; public Save input receives no such repair. Users adopt a newer bundled template only by deleting the old Automation and creating it again. Registration may still remove obsolete noncurrent graphs and exclusively owned Schedules left by pre-contract failed/draft attempts.
- Save writes Automation identity, the complete graph, Task changes, Scheduler rows, bindings, lifecycle-compatible admission state, and replaced-graph cleanup in one SQLite transaction. Replacement allocates a collision-free temporary graph sequence and removes every noncurrent graph before normalizing the new current graph to sequence 1, so databases upgraded with a retained failed draft still Save successfully. Any error rolls back all Save writes, so a first Save creates nothing and an edit leaves its previous graph active.
- Chat uses `plan_automation_save` followed by later-confirmed `save_automation`. Before Save, the displayed plan and private confirmation state expose no Automation/graph identity, URL, runtime resources, or draft terminology. Confirmation consumption and Save commit atomically.
- A rejected browser Save renders a prominent `Save did not apply` message and expands the actionable setup list. For an unready GitHub SDLC graph, the list names connected GitHub authentication, at least one GitHub Authorized User, and either a project GitHub repository URL or a GitHub remote in the project's local checkout; the failed attempt still creates no Automation, Task, or Schedule rows. The maintained GitHub template owns valid defaults for its browser fields (`instructions`, `labels`, `base`, and `draft`), and Save applies the same required-value, type, length, list-size, and forbidden `openvibely:` label-prefix validation used by equivalent custom nodes before any persistence.

## Graph Contract

- Blank is an open custom builder, not a preset assembler. Users can add supported Schedule, Task, Native Alert, GitHub action/gate, and Outcome nodes and connect deterministic supported handoffs.
- A Schedule is a substantive Task plus a Scheduler row targeting that same Task. `Schedule → Task` means scheduled Task A completes before OpenVibely activates distinct follow-up Task B. Custom Schedule handoffs are represented only by graph edges; public candidates containing `target_node_key` are rejected, while trusted reopen removes obsolete topology-derived metadata from older saved graphs.
- Maintained Native and GitHub recurring loops use one Schedule node per substantive scheduled Task. Vision Driver also uses one Schedule node rather than a schedule relay linked to a duplicate same-purpose Task.
- The maintained GitHub SDLC Automation owns complete Offering Manager, finder, Dev Inbox, and Loop Auditor prompts plus daily discovery, hourly inbox, and weekly auditor cadences. The maintained Native SDLC Automation likewise owns complete Vision Suggestions, finder, Notification Inbox, and Loop Auditor prompts aligned with the bundled Native bootstrap contract: focused notification-only discovery with stable idempotency, approved-notification inspection and atomic claim/link processing, bounded recovery, and approval-gated audit reporting. Neither template reads or requires its bootstrap skill at runtime. Initial registration hydrates the exact bound Task/Schedule configuration into the point-in-time graph without mutating the bound Task prompt; later software releases do not rewrite existing saved Automations.
- Graph connections leave the source node's unlabeled right output handle and enter the target node's unlabeled left input handle. Missing, reversed, or same-kind ports are rejected through the public Save path. Legacy saved connector metadata is normalized only when reopening an existing saved graph.
- Unsupported handoffs, unsafe configuration, ambiguous task parents, executable cycles, invalid conditions, foreign-project references, and unavailable capabilities fail closed before resource effects.
- The hidden Workflow subsystem is not exposed as an Automation node.

## Runtime And Lifecycle

- Newly created runnable Tasks remain non-admitted Backlog rows inside the Save transaction until the graph, resource membership, configured categories, and topology are ready. New or existing roots that end Save as pending and Active are submitted through the existing worker only after commit, including Backlog-to-Active changes and completed roots reset to pending.
- Saving while paused or archived preserves that lifecycle. It does not enable schedules, resume work, or reacquire active ownership. Pause and Archive atomically demote all current-graph pending Active Tasks, including definition-bound roots, entered downstream children, and activity-created child Tasks, to Backlog. Pause records only activity children that were Active at deactivation, so Resume can restore those exact Tasks once without admitting intentionally Backlog work; Archive discards that admission and remains non-resumable. Ordinary dispatch rechecks current graph lifecycle in the same immediate claim transaction, demotes and rejects a Task when deactivation won the race, and preserves stale replaced-graph domain Tasks as independent work.
- Active root Tasks added or changed from Backlog to Active while paused remain non-admitted until explicit Resume. Downstream task handoffs recheck the current graph and lifecycle in the same transaction: Paused and Archived Automations persist the causal handoff as pending Backlog work without submitting it, Resume derives effective runnable state from the exact current topology, entered transition, or durable paused activity-child admission so Schedule-derived `task`, `github_inbox`, issue-specific, and generic activity-created children are admitted once when eligible, ordinary configured Backlog Tasks remain deferred, and Archived Automations remain non-resumable. Custom Task fan-out treats queued or running children as branch-local contention: each busy branch records its blocked projection and remains unchanged while later independent siblings are still attempted, and the combined completion result continues to report the busy condition.
- Unrelated Automation edits preserve each retained Scheduler row's exact `run_at` and `next_run` when cadence fields are unchanged. Maintained registration reruns leave the saved schedule configuration and configured enabled or disabled intent untouched; lifecycle state keeps paused or archived schedules physically disabled until explicit Resume restores that saved intent.
- Ordinary queued roots atomically claim and retrieve their full authoritative persisted Task plus current Automation context in one SQLite immediate transaction. A queued Task ID is only an admission hint: replacement prompt, selected Agent, topology, and graph identity are loaded at the claim linearization point, so Save and dispatch cannot mix old Task behavior with a replacement graph. Definition-only root provenance creates and terminalizes an execution-scoped work item on the admitted Live graph; prepared Automation dispatches retain their exact immutable envelope.
- Archived Automations retain disabled exclusive schedule provenance using archived owner rows. Archived edits can therefore remove obsolete Scheduler rows, and deleting an archived Automation removes all exclusively owned Scheduler rows while preserving Tasks. Deletion rejects both scheduler in-flight work and ordinary running Task executions linked through exact Automation activity resources; an ordinary execution stops blocking once terminal. An orphaned `running` invocation left after ordinary Task deletion has already cascaded away its dispatch and execution no longer blocks deletion forever.
- Automation GitHub runtime paths use the current project's repository URL when configured and otherwise resolve a GitHub remote from the project's local checkout. This same URL-first/local-remote boundary is used by Save readiness, issue tools, issue-specific Task creation, task-scoped PR open/replace, and external-state refresh, so Save cannot accept a repository that runtime later rejects. Model-supplied repository overrides remain ignored. GitHub Dev Inbox `create_task` validates the exact current execution, discovered issue work item, active graph, and required inbox-to-task-to-PR topology before inserting a Task. Task insertion, goal insertion, and issue-work-item provenance commit atomically under a stable work-item activity claim, so failures leave no unbound Task and concurrent or repeated calls reuse one canonical implementation Task. Compiler-created and issue-specific Tasks retain a durable Automation-origin marker after graph replacement deletes old projection. Migration `124` and the pre-replacement Save correction repair only prior feature-created issue Tasks carrying the exact `work-item:<id>:implementation-task` activity, child-task relation, and Task/legacy-implementation node provenance; unrelated pre-feature and generic `create_task` resources are not inferred or registered. Idle web, queued/channel, and failed-retry Task Thread paths load both the Task and causal Automation context before runtime composition; when replacement has removed that context, the durable marker installs an origin-only fail-closed guard, while already-prepared old bindings are revalidated against the current active graph. Task-bound Discord, Slack, and Telegram `create_task` handlers use the same hardened creation path. Every Automation-bound GitHub write is intercepted before generic runtime fallback and stale origin/binding contexts cannot invoke provider mutations. Issue creation, PR opening, and guarded PR branch replacement additionally require every caller binding to authorize the exact mutation topology, and task-scoped PR mutations must target the causal caller Task. Issue-creation duplicate protection never lists, searches, fetches, or compares existing GitHub issues. It uses a durable local project/repository/normalized-title-fingerprint record with `reserved`, `dispatched`, and `completed` states. Only a definitively pre-dispatch reservation may expire or be released. Once GitHub may have mutated, an ambiguous outcome remains permanently fail-closed. A successful returned issue number and an IDs-only snapshot of the exact OpenVibely projection source are recorded in the claim; the claim stores no GitHub title, body, comment, or instruction content. A completed claim is reusable only by the same project, Task, and complete Automation/graph/source-node binding set, while invocation and execution IDs may change for a legitimate later run. Cross-Automation, replacement-graph, source-node, and Task collisions fail closed without another provider mutation or GitHub lookup. The issue activity, work item, issue transition, and waiting assignment transition are finalized with a bounded context independent of caller cancellation. Same-source same- and later-execution retries idempotently repair any missing projection and return the locally recorded canonical issue only after every stored source binding records its expected issue and assignment transitions. A resource already present on one causal binding is treated as reconciliation evidence and cannot short-circuit completed-claim repair for the remaining bindings. Deleted or incomplete source graphs remain reconciliation errors rather than successful no-ops. Historical claims without trusted projection-source IDs remain fail-closed. Assigned-issue inbox reads remain a separate human-gated flow and are not used for deduplication.
- Native Alert approval never authorizes merge, release, or deployment. GitHub assignment, review, merge, release, and deployment remain human-controlled.
- Runtime state is shown only in the full-width Live graph, whose canvas grows to fill the remaining viewport height. Only Schedule and Task nodes with exact current task bindings navigate, and they open that existing project-scoped Task; nodes without runtime resources are inert. Polling, SSE, visibility, and manual external refreshes each use an independent request source plus one browser request-generation guard, so genuinely overlapping responses can complete out of order without an older response overwriting newer Live state.
- The Automations portfolio reads every saved project Automation rather than a fixed first page, excludes unsaved publication-era draft shells at the repository boundary, and retains deterministic updated-time ordering and browser-side search across all cards. Periodic health reconciliation likewise excludes unsaved shells and traverses every saved Automation in bounded primary-key pages, so health updates cannot leave rows beyond the first 100 permanently stale. Migration `122` deletes Scheduler rows created by failed publication attempts while their exact create-step journal still exists, preserving current and unrelated schedules plus every backing Task. Migration `125` deletes schedules exactly owned by draft-only or noncurrent graphs before removing those identities/graphs, then normalizes the current sequence/state and adds the lifecycle admission store for paused activity-created Tasks. Migration `126` adds the local created-issue number used by Automation issue deduplication; it stores no GitHub title, body, comment, or instruction text. Migration `127` adds the durable mutation state and conservatively marks pre-upgrade numberless claims as dispatched uncertainty rather than reopening them. Migration `128` adds the IDs-only projection-source snapshot for new claims; historical rows remain without inferred provenance and therefore fail closed if projection repair is requested.

## Regression Coverage

Current regressions cover:

- Browser-local Template, Describe It, Blank, and Edit flows; atomic Save while running; one-current-graph replacement; and injected first-Save/replacement failures that prove complete rollback with no partial Tasks, Scheduler rows, Automation identity, or graph replacement.
- Point-in-time maintained registration for active, paused, and archived Automations, including changed template node attributes, approval topology, names, and supplied resource IDs; reruns preserve the current graph/runtime projection, lifecycle, owned schedule, bindings, and existing work while cleanup removes retained failed/draft graphs and their obsolete exclusively owned Schedules without deleting backing domain Tasks. Registration now persists complete valid defaults for maintained Native/GitHub nodes before exact Task/Schedule configuration is overlaid. Edit coverage empties action/gate configuration on a production-registered snapshot, proves trusted reopen restores only missing canonical keys, performs an explicit replacement Save while retaining the bound Task/Schedule, and separately proves malformed public candidates remain rejected.
- Replacement Save cleanup of schedules owned by both the expected current graph and any additional retained draft graph, while retaining the backing domain Tasks and leaving exactly one current graph.
- Automation-owned prompt coverage for both maintained SDLC adapters: Native Vision Suggestions, all three Native finders, Notification Inbox, and Native Loop Auditor enforce the bundled Native bootstrap behavior without loading the skill; GitHub Offering Manager, all three GitHub finders, Dev Inbox, and GitHub Loop Auditor retain their integration-specific contracts. Cadence coverage and source-level regressions forbid runtime dependencies on `builtinskills`, `SKILL.md`, or global skill support files. Existing saved Automations continue from their current persisted graph.
- Public GitHub autonomous-SDLC guide alignment coverage requires Offering Manager discovery guidance to forbid listing, searching, or inspecting GitHub issues for duplicate detection, identifies trusted local Automation state as the server-owned duplicate boundary, and rejects the prior visible-work search wording.
- Public Native autonomous-SDLC guide alignment coverage requires the bundled six-role contract: Vision Suggestions, Bug Finder, Optimization Finder, Redundancy Finder, Notification Inbox, and Loop Auditor as separate visible scheduled tasks with daily discovery, hourly inbox, weekly audit, notification-only discovery, atomic task linkage, recovery, and explicit `native_sdlc` registration node keys. It rejects the stale two-task suggestion-producer/inbox setup wording.
- No pre-commit root execution, production provenance lookup, atomic authoritative queued-root claim after a barrier-controlled concurrent Save changing prompt/Agent/topology, completed root Live projection, paused/archived pending current-graph Task demotion, activity-only issue-specific and generic created-child demotion, final dispatch rejection after lifecycle deactivation, exact once-only paused activity-child admission on Resume, paused root/deferred-child admission, exact entered-transition admission for Backlog-configured Schedule-derived `task` and `github_inbox` children, post-commit root submission, barrier-controlled Pause/Archive after child activation but before submission, lifecycle-gated downstream handoffs, and deterministic task handoffs/fan-out. Fan-out coverage exercises both edge orders with a queued or running sibling, proving the busy branch records one blocked transition without duplicate submission while every eligible sibling activates and submits exactly once across repeated completion callbacks.
- Archive then delete, archive then schedule removal, paused/archived Save, unchanged-cadence `run_at`/`next_run` preservation, individually disabled maintained registration, paused point-in-time registration reruns, and configured enabled/disabled schedule restoration on Resume. Deletion coverage reproduces an ordinary Automation-bound running Task execution with no scheduler dispatch, records its production-shaped work item, activity, transition, and execution resource, and proves deletion preserves the graph and projection until that execution becomes terminal. Adjacent coverage proves a `running` invocation orphaned by ordinary Task deletion no longer blocks removal after its dispatch/execution have cascaded away, while a genuine in-flight scheduler dispatch continues to reject deletion and preserve recovery rows.
- GitHub SDLC browser Save coverage reproduces both sides of setup validation: an unconfigured project remains in the builder with actionable authentication, Authorized User, and URL/local-remote repository guidance and no created resources; a configured project matching the public Channels UI and project settings saves successfully without any hidden legacy inbox row, including the exact action settings emitted by the browser form. A separate atomic Save matrix mutates maintained issue and pull-request settings with wrong types, instructions/base/label values above their limits, more than ten labels, and a case-insensitive forbidden `openvibely:` label prefix; every case fails validation and leaves zero Automation, Task, and Schedule rows.
- Automation rename propagation to bound Task titles; strict public browser Save rejection of mismatched `automation_type`, explicit empty maintained-node names, compatibility-only custom `implementation` roles, invalid Task/Schedule categories, explicit custom Schedule targets, and missing or reversed ports; preservation of valid user-selected connected Task categories in both graph metadata and Task rows; plus narrowly scoped trusted reopened-graph cleanup for legacy roles, connectors, and obsolete custom target metadata.
- Schedule-owned Task semantics across custom, maintained, Describe It, template, and Chat paths.
- Live-only detail, exact Task links, removed auxiliary routes, absence of version/history compatibility copy, and a real-Chrome overlapping request race that holds the oldest response server-side, mounts the newest response first, then proves the released stale response is rejected.
- Native Alert and GitHub lifecycle boundaries; URL-first/local-Git-remote Automation repository resolution for issue tools, issue-bound Task creation, task-scoped PR open/replace, and external refresh; fail-closed current-graph topology and causal-task checks for issue/PR mutations; idle web plus queued/channel plus failed-retry follow-up runtime preparation; graph-replacement origin-only and stale-binding GitHub write guards with zero provider mutations; durable origin marking for issue-specific Tasks; migration and replacement-time repair restricted to the exact prior feature-owned issue-task signature with unrelated generic resources left untouched; atomic exact-execution issue-to-Task provenance; sequential and concurrent canonical Task reuse per issue work item; same-execution issue mutation idempotency; and local concurrency-safe cross-run issue duplicate prevention without reading or persisting existing GitHub issue content. Post-mutation regressions advance beyond lease expiry and prove both an ambiguous provider error and caller cancellation after provider success cannot produce a second issue. The cancellation regression requires the completed issue number plus the exact completed source activity, issue/task/execution resources, one issue work item, one issue transition, one waiting assignment transition, and the waiting assignment position. Injected failures before work-item creation, between the issue and assignment transitions, and after the first of two causal Automation bindings prove same- and later-execution retries repair the original projection without duplicate activities, resources, work items, transitions, or provider calls. The multi-binding regression specifically proves a completed resource on the first Automation cannot return `reused` until the second Automation has its canonical issue work item, issue transition, waiting assignment transition, and assignment position. Cross-Automation normalized-title and explicit graph-replacement regressions prove a completed claim cannot be adopted by a different Automation or saved graph and causes no second provider mutation; deleted-source repair coverage proves a missing original graph cannot return success after recording nothing. The stored recovery snapshot is asserted to contain OpenVibely IDs but no issue title or body.
- Describe It capability validation rejects or repairs generated maintained/custom GitHub graphs when connected authentication, Authorized Users, or both project-URL and local-Git-remote repository resolution are unavailable, while accepting the same valid graph when that public setup boundary is ready.
- Migration `122` upgrade cleanup for a failed publication-created schedule, with current and unrelated schedules preserved; migration `125` cleanup for a published graph plus retained failed draft and a draft-only identity, including exact obsolete schedule deletion with every backing Task preserved; replacement Save with a colliding legacy draft version 2 and owned schedule; point-in-time registration cleanup of retained draft-owned schedules while preserving the current graph; portfolio coverage proving 101 saved Automations remain visible even when newer draft shells and a foreign-project Automation exist; and health-reconciliation coverage proving one periodic pass evaluates all 102 saved Automations across the 100-row batch boundary.

## Implementation State

The current task branch contains the complete Automation Graphs implementation plus the latest audit repairs. This implementation turn does not claim a clean audit. The required next step is a separate fresh strictly read-only review of the complete checkpoint and requirements.

## Validation

Current implementation-turn evidence from 2026-07-25:

- `make templ` completed with zero generated updates; touched Go files are `gofmt` clean and `git diff --check` passes.
- `go build ./...` and `go vet ./...` pass; desktop linking emits only the documented non-failing newer-SDK macOS warnings.
- Focused maintained-registration and runtime regressions pass under the race detector. They prove first registration hydration, template node-key/name/type/role/position and approval-edge changes, changed names and resource IDs, Active/Paused/Archived lifecycle states, retained failed/draft cleanup, current graph/runtime/binding/schedule preservation, paused schedule intent, and concurrent initial-snapshot invocations.
- Focused GitHub issue-creation regressions pass under the race detector. The post-mutation matrix proves issue creation performs no existing-issue get or list operation; concurrent normalized-title equivalents create one issue; an ambiguous post-dispatch provider error remains fail-closed after lease expiry; and a successful issue number plus IDs-only projection source remain durable when the request is canceled after provider success. Cancellation-independent finalization records the original completed activity, issue/task/execution resources, canonical waiting work item, issue transition, waiting assignment transition, and assignment position. Injected failures before work-item creation and between the issue and assignment transitions are repaired by same-source same- and later-execution retries, including a retry with its own stale pending reservation, with exactly one provider create and no duplicate local projection rows. Cross-Automation normalized-title and explicit replacement-graph retries fail closed without another provider mutation or projection adoption, while direct deleted-source repair fails instead of reporting success after recording zero bindings. A two-Automation causal context now injects failure after the first binding projects, proves the second binding is still absent, and then proves retry repairs both canonical projections before returning `reused`, with exactly one provider create. Database coverage applies migration `128`, proves historical claims receive no inferred projection source, and retains migration `127` coverage for pre-upgrade numberless uncertainty and completed issue numbers.
- The focused health regression passes under the race detector and proves a single periodic reconciliation traverses all 102 saved Automations across the 100-row batch boundary; adjacent persisted-event and stale-failure health regressions also pass. The focused custom Task fan-out regression also passes under the race detector and proves queued-first and running-last busy branches preserve their blocked projection and error reporting without suppressing or duplicating the eligible sibling's activation and worker submission.
- The GitHub SDLC Save regressions pass under the race detector. The configured case matches the reported database shape: PAT mode, a public GitHub Authorized User, a project repository, no hidden legacy project-inbox row, and the exact `instructions`, `labels`, `base`, and `draft` settings emitted by the browser. It saves the Automation, Tasks, and Schedules successfully. The unconfigured case remains rejected with actionable public setup guidance and no partial resources. The maintained-setting atomic matrix proves invalid instructions, labels, base, and draft types; over-limit instructions, labels, label count, and base; and a case-insensitive forbidden `openvibely:` label prefix all fail before persistence. Repository-resolution regressions also pass under the race detector and prove Save, issue creation, issue-bound Task creation, task-scoped PR open/replace, and external refresh prefer the project URL and otherwise use the project checkout's GitHub remote while ignoring model-supplied repository overrides. The generated maintained and custom Dev Inbox prompts describe that same fallback, use public Authorized Users, and do not instruct discovery Tasks to inspect repository-wide GitHub issues for duplicate detection. The checkout referenced by `/Users/dubee/.openvibely/openvibely-test.db` has a valid GitHub `origin`; the original database was not mutated.
- The bundled GitHub bootstrap regression passes under the race detector after reproducing the stale shipped guidance. It proves the installed Dev Inbox prompt forbids `source_github_repo_url`, describes configured-project-URL-first/local-GitHub-remote fallback, and makes server-owned trusted local state rather than GitHub issue-corpus searches responsible for Automation issue deduplication. Existing bootstrap catalog and documentation-alignment contracts also pass.
- The public-guide alignment regression first failed on the stale Offering Manager recommendation to search or inspect visible work, then passes under the race detector after the guide was aligned with the maintained prompt and bundled skill. A tracked-source search finds no remaining positive user-facing guidance in this class; remaining old phrases occur only in negative regression assertions.
- The Native maintained-prompt regression first failed because every Native scheduled role received only the generic one-line `Run the ... role` fallback and no Automation-owned Native prompt source existed. It now passes after adding self-contained Native Vision Suggestions, finder, Notification Inbox, and Loop Auditor prompts aligned with the bundled bootstrap skill's operational contract. Focused Native template tests, the full `internal/service` suite, `go build ./...`, `go test ./... -count=1 -timeout 300s`, and `go vet ./...` pass; the template source contains no `builtinskills` or `SKILL.md` dependency.
- The Native public-guide regression first failed because the guide omitted the six maintained loop roles and described only a suggestion producer plus approved-notification inbox. It now passes under the race detector after the guide was aligned with the bundled Native bootstrap contract: six separate visible scheduled tasks, maintained cadences, notification-only discovery, approval-gated atomic inbox processing and recovery, weekly loop auditing, and explicit `native_sdlc` resource registration. The repair changed `docs/openvibely-native-autonomous-sdlc-user-guide.md`, `internal/agentskills/catalog_test.go`, and this status ledger. A tracked Markdown search finds no remaining stale two-task Native guidance. `make templ` reports zero generated updates; `go build ./...`, `go vet ./...`, `go test -race ./internal/agentskills -run TestNativeAutonomousSDLCDocsAlignWithBootstrapSkill -count=1`, and `TMPDIR=/private/tmp go test -p 1 ./... -count=1 -timeout 300s` all pass; build/test output contains only the documented non-failing macOS linker warnings.
- The registered-maintained Edit/Save regression passes under the race detector. It proves new Native registration persists canonical action/gate defaults, simulates an older snapshot by emptying those stored configs, reopens it with only absent keys hydrated, explicitly saves a replacement graph, and retains the exact bound Task and Schedule. The same regression proves a registered GitHub graph reopens without semantic validation errors and carries canonical `labels`, `draft`, and approval settings, while a malformed public candidate with a removed approval method remains rejected. Focused GitHub issue-runtime regressions also pass with idempotency keys computed from the newly persisted maintained empty-label configuration.
- The full deletion matrix passes under the race detector. It proves an ordinary Automation-bound running execution with no dispatch row blocks deletion through its activity resource, preserves its graph/projection/execution on rejection, and permits deletion only after the execution becomes terminal. It also proves an orphaned invocation with no surviving dispatch or execution remains deletable and a genuine scheduler dispatch remains protected for restart recovery. A transactionally consistent temporary copy of `/Users/dubee/.openvibely/openvibely-test.db` deleted both reported stale Automations with foreign keys enabled and returned no `foreign_key_check` violations; the original database was opened read-only and left unchanged.
- `TMPDIR=/private/tmp go test ./web/templates/pages -run 'TestAutomationGraphAndNavigationInChrome' -count=1 -timeout 180s` passes against real Chrome, including the held-older/released-after-newer Live refresh overlap.
- `TMPDIR=/private/tmp go test -p 1 ./internal/database ./internal/repository ./internal/service ./internal/handler ./internal/server ./web/templates/pages -count=1 -timeout 300s` passes all affected packages.
- `TMPDIR=/private/tmp go test -p 1 ./... -count=1 -timeout 300s` passes every package.

The atomic Save correction removes publication-attempt and publication-step persistence, candidate hashing, plan revisions, compiler contract versions, and browser recovery state. Repository regressions inject Task/Schedule write failures and prove SQLite rollback leaves either no new Automation or the previous graph and resources unchanged.

The implementation and tracked specification/status documentation describe the same self-contained GitHub SDLC Automation prompt defaults, recurring cadences, point-in-time registration snapshots, browser-local creation paths, atomic Save contract, lifecycle admission, exact issue-to-Task provenance, public Authorized Users readiness, and URL-first/local-Git-remote repository boundary. Generated maintained and custom Dev Inbox prompts now describe that same repository fallback instead of claiming an explicit URL is mandatory. The Automation has no runtime dependency on the bootstrap skill package.

Managed memory is intentionally excluded from completion evidence and reconciliation in this implementation turn, as requested by the user.

The latest public-guide correction aligns the Offering Manager example with the maintained prompt, bundled bootstrap skill, and trusted local Automation deduplication boundary: discovery roles must not list, search, or inspect GitHub issues for duplicate detection. The latest bootstrap-guidance correction aligns the bundled GitHub setup skill with the enforced project-repository boundary and canonical maintained prompts. Dev Inbox now sets only the exact discovered issue number, never a model-supplied repository override; the server resolves the selected project's configured URL first and its local checkout's GitHub remote second. Offering/finder guidance now relies on trusted local Automation duplicate protection and explicitly forbids GitHub issue-corpus searches. The latest registered-snapshot correction gives template creation and maintained setup registration one canonical source of complete valid node defaults. Registration overlays exact bound Task/Schedule values before publication, so newly registered Native and GitHub graphs are immediately editable. Trusted Edit reconstruction fills only absent keys for older maintained snapshots, preserves explicit stored values, remains browser-local until explicit Save, and does not weaken strict public Save validation. The latest maintained-configuration correction gives canonical templates valid action/gate defaults and routes maintained GitHub `instructions`, `labels`, `base`, and `draft` through the same strict semantic validator as equivalent custom nodes. Invalid required values, types, limits, and forbidden label prefixes now fail before the atomic Save transaction can create any resource. The authoritative specification and generated Automation prompts now use public Authorized Users plus configured-URL-first/local-Git-remote fallback, and discovery prompts rely on trusted local duplicate protection rather than issue-corpus inspection. The prior Save correction accepts the maintained GitHub template's exact browser-owned action settings and evaluates readiness from the same public controls the user configures: connected PAT/App authentication, at least one GitHub Authorized User, and either a project repository URL or a GitHub remote in the project's local checkout. The same URL-first/local-remote resolver now governs Automation issue tools, issue-specific Task creation, task-scoped PR operations, and external refresh, so Save cannot approve a repository boundary that runtime later rejects. Model-supplied repository overrides remain ignored. The latest deletion correction extends the in-flight guard beyond scheduler dispatch rows to exact Automation activity resources. A running ordinary Task execution now preserves its Automation graph and projection until terminal, while a genuinely orphaned invocation with no surviving dispatch or execution remains safely deletable. The latest GitHub retry correction treats an existing issue resource on one causal binding as reconciliation evidence rather than returning `reused` immediately. The runtime still acquires the trusted completed claim and runs its stored all-binding repair, so a partial multi-Automation projection cannot report success until every original binding has the canonical issue work item, issue transition, waiting assignment transition, and assignment position. The broader duplicate correction removes the existing-issue lookup client method and runtime interface entirely. Automation issue creation now serializes by a local project/repository/normalized-title fingerprint and transitions the durable claim from `reserved` to `dispatched` immediately before the provider call. Only `reserved` claims can expire or be released. A provider error after dispatch leaves a permanent local uncertainty claim. A successful provider response records the returned issue number plus an IDs-only OpenVibely projection source, then finalizes the issue activity, canonical work item, issue transition, and waiting assignment transition through a bounded cancellation-independent context. Completed-claim reuse compares the current project, Task, and full Automation/graph/source-node binding set with that stored source while allowing invocation and execution IDs to change for same-source later runs. A mismatch remains a local reconciliation error without a GitHub read or mutation. If either projection boundary fails after the number is completed, a same-source same- or later-execution retry uses only that trusted local source to repair the original projection idempotently, and returns the canonical number and trusted repository URL only after every expected binding records the issue and assignment transitions. Deleted or incomplete source graphs fail repair rather than returning success after a no-op. Concurrent same-source callers remain blocked and later same-source equivalents reuse the local canonical issue without reading GitHub or invoking create again. The latest fan-out correction treats `ErrAutomationChainChildBusy` as a branch-local result inside the persisted handoff loop. It retains the blocked projection and returns the accumulated busy error after all siblings are attempted, while non-busy topology, configuration, lifecycle, and provenance errors remain fail-fast. The regression first reproduced a queued first child leaving its later sibling blocked, then proved queued-first and running-last orders both activate and submit the eligible sibling exactly once across repeated callbacks. The health-reconciliation correction remains bounded primary-key keyset pagination over every saved Automation, including rows beyond the 100-row page boundary. The latest Native public-guide correction repairs the final audit's stale two-task setup. The guide now matches the bundled maintained bootstrap and Automation-owned prompts by documenting all six scheduled roles, their discovery/inbox/audit cadences and boundaries, atomic approved-notification linkage and recovery, and explicit `native_sdlc` registration using only the six canonical node keys. The regression rejects the old suggestion-producer/inbox wording, and no legacy registration, inference, migration, or backfill behavior was added. This ledger does not claim a clean final audit: a separate fresh strictly read-only audit-only turn is still required after this implementation checkpoint.
