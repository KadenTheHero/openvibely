---
name: chat_thread_system
type: project
created: 2026-05-09
updated: 2026-07-04
source: consolidation
source_id: memory_consolidation_2026_07_01; b841541e0cee6d7b07b2702ae5df4c4e; 11090658cf527d60fc9af73c9c89b573; 11090658cf527d60fc9af73c9c89b573:deb5b3eb6be4e158; 11090658cf527d60fc9af73c9c89b573:8c8070c575f6beaf; 11090658cf527d60fc9af73c9c89b573:d5fadbafd2d68c54; 11090658cf527d60fc9af73c9c89b573:8577b6cab60470ef; 11090658cf527d60fc9af73c9c89b573:4d60ccca6036e865; 11090658cf527d60fc9af73c9c89b573:403f605ebc98b716; 11090658cf527d60fc9af73c9c89b573:516dad24496afae3; 11090658cf527d60fc9af73c9c89b573:dbdce7576a07d893
confidence: high
title: Chat and Task-Thread Behavior
---

Interactive Chat bypasses worker capacity limits. Task-thread follow-ups respect worker limits and use `processStreamingResponse` with `IsTaskFollowup=true`.

Queueing and steering facts:
- Queueing, steering, and explicit stop are distinct product behaviors. Normal Send during an active Chat or task-thread response queues a next-turn input; steering is an explicit in-progress action applied only at OpenVibely-owned provider-call/model-step boundaries; the active composer Stop action cancels the current execution instead of queueing input.
- Queueing and steering use durable `thread_inputs`; `executions` represent real model runs, not parked composer state.
- Queued input promotion claims the pending input while creating the next execution/task and keeps FIFO ordering durable.
- Queued messages with attachments store the pending upload `attachment_session_id` on the durable `thread_inputs` row; final execution attachment records are created when the queued input is promoted and the attachment session is processed.
- Chat and task-thread composers share pending attachment upload behavior: repeated file/screenshot drops append into the current `attachment_session_id`, duplicate browser filenames are preserved with unique pending filenames, and the existing three-files-per-message limit applies across repeated drops.
- Task-thread sends made while a task execution is running, including before first assistant output or while initial lifecycle routing/hooks are still running, queue as next-turn input rather than running inline or duplicating messages.
- Steering rows target an active execution with an `expected_turn_id` guard and use two-phase consumption so failed/cancelled provider steps can recover input.
- Current merged steering instruction formatting passes through the user's steering text trimmed, without wrapping it in additional “latest user instruction” wording.
- Prepared/in-flight steering clears `expected_turn_id` while the row can still have `input_status='pending'`; pending-list queries and UI should exclude those rows until committed, restored, or requeued.
- Task-thread pending composer rows are reconciled from authoritative server-side pending-input fragments on relevant live task/execution/input events.
- Provider failure requeues prepared steering; retry restore returns it to guarded steering. Durable cleanup/finalization writes use non-cancelled contexts where needed so pending steering and executions are not stranded after request cancellation.
- Chat/task-thread success defers completion when pending steering exists; if follow-on steering preparation fails or finds no claimable steer, pending steering for that execution is requeued before terminal failure.
- Thread history for future calls is rebuilt from `executions.PromptSent` and cleaned `executions.Output` as plain turns rather than provider-native tool-use messages.
- Failed task executions remain visible terminal history rows. When no replayable assistant output exists, replay preserves the prompt with explicit failure metadata rather than leaving a trailing user-only turn.
- Initial task executions that report final `[STATUS: FAILED | ...]` persist assistant failure transcript/output while marking execution and task failed, so follow-up context can replay the failed turn chronologically.
- Task failure finalization does not clear execution diff output; diff capture/update is a separate post-processing path.

Routes, modes, and runtime actions:
- `/chat` is the global/project-level orchestrator; the task detail Thread tab is task-specific.
- `/chat` supports Orchestrate and Plan modes. Plan is read-only and disables marker execution.
- Canonical chat capabilities live in `internal/chatcontrol/registry.go`.
- Runtime tools and marker processing are mutually exclusive only after runtime tools are actually injected into a provider-capable request; injected runtime tools set `ProcessMarkers=false`, while runtime-tool-incapable provider/auth paths must keep legacy marker fallback available.
- Status-marker parsing treats `[STATUS: FAILED | ...]`, `[STATUS: COMPLETE | ...]`, and related markers as terminal control markers only when they are the final standalone non-empty line. Literal marker text in prose, code spans, code fences, bullets, quotes, examples, or lines with trailing explanatory text must not classify an execution as failed/completed.
- Chat output cleaning preserves status/tool marker text inside inline backtick code spans while stripping real standalone control markers outside inline code.
- Failed-task history replay and terminal-marker fixes are provider-neutral shared-layer behavior, not provider-specific fixes.
- Task creation can happen through marker compatibility, runtime action surfaces, or local app APIs depending on active runtime. When `[CREATE_TASK]` block markers are the exposed creation path for a turn, task-management guidance should use that contract; non-running/backlog task creation should set `"category": "backlog"`.
- Chat task creation distinguishes Agent definitions from model configs: `agent` names a selectable/enabled Agent, while `agent_id` is internal model-config selection.
- Chat-control task/schedule automation should respect schema semantics: task `priority` uses `1=Low`, `2=Normal`, `3=High`, `4=Urgent`; scheduled task `days` values are short weekday keys; `edit_task` needs a real task ID; `schedule_task` moves the target task into `scheduled`, so bootstrap flows needing immediate work should schedule then explicitly execute when appropriate.
- `send_message` is an Orchestrate-mode write runtime tool for sending outbound Slack, Telegram, Email, and Discord messages through configured project channel targets. It is runtime-tool-only; no `[SEND_MESSAGE]` marker fallback is currently supported. Normal task execution and task-thread follow-ups expose/advertise a narrow default `send_message` runtime only when the selected provider/auth path supports runtime tools, so tasks can send completion messages without requiring an explicit agent-tool grant. Runtime-tool-incapable provider/auth paths still cannot call it and must retain legacy task-creation marker fallback.

Swarm task facts:
- Swarm tasks are a parent/child task orchestration mode with planner, worker, reviewer, and merger (formerly "integrator", renamed 2026-07-04 with legacy compatibility — see below) children plus parent summaries, task-detail swarm panels, and a `create_swarm_task` runtime tool.
- The swarm parent task itself does not execute an LLM call in the normal run; it is an orchestration/state container that stores the original goal, tracks swarm phase/status, creates/starts child tasks, and receives the final result (merger output copied onto it, or direct completion when merger is disabled). All LLM work happens in the planner, worker, reviewer, and merger child task executions. Parent task-thread follow-ups are the one exception: sending a follow-up to the parent is routed into swarm coordination (affecting child prompts/reruns) rather than triggering a parent-level LLM execution.
- Planner completion parses the latest completed planner output as JSON, tolerating surrounding transcript/tool text, and applies planner output idempotently so duplicate planner callbacks do not create duplicate workers. The planner output contract is a single JSON object with `workers` plus optional `reviewer_prompt`, `integrator_prompt`, and `notes`; no separate task consumes the raw JSON because `SwarmService` parses it and creates/starts child tasks from it.
- Planner parsing (`ParsePlannerOutputJSON`) tries strict full-string JSON first, then falls back to scanning fenced/balanced JSON objects in true text order and accepts the LAST candidate that decodes with a non-empty `workers` array, skipping unrelated tool-call/result JSON in the transcript. Fixed 2026-07-02: it previously returned the *first* matching candidate, which could select a stale/earlier example plan instead of the model's real final answer when a planner/follow-up transcript contained multiple valid workers-shaped JSON objects; the fix removed a separate fence-first extraction pass (which broke text ordering) so fenced and balanced-object candidates are now scanned in one text-ordered pass, and `TestParsePlannerOutputJSONPrefersFinalPlannerObject` in `internal/service/swarm_service_test.go` covers an earlier stale plan followed by a later final fenced plan. It still fails if no JSON object is present, the JSON is malformed, `workers` is empty, or decoded fields fail swarm validation (too many workers, missing required title/prompt, invalid isolation/scopes).
- Integrator completion copies the latest fresh completed integrator output/diff into an idempotent parent execution and marks parent merge status pending when a final diff exists.
- Parent task-thread follow-ups are routed through swarm coordination instead of ordinary parent executions: affected workers can be targeted by `task_id`, unaffected completed workers carry forward, and reviewer/integrator phases rerun when needed.
- Swarm child follow-ups are role-specific: worker follow-ups advance generation and require review/integration, reviewer follow-ups invalidate integration only, and integrator follow-ups invalidate only final integration/result.
- Child cancellation, streaming cancellation, and Active-to-non-active category moves notify swarm orchestration. Pending swarm children moved out of Active are cancellable runnable work.
- `SwarmService.RecomputeParentStatus` and `OnChildCompleted` guard against stale planner/worker/reviewer/integrator completions overriding fresher pending/running work or copying stale final output.
- Queued swarm child follow-up metadata advances only when a real child follow-up execution starts; direct sends apply routing after execution creation, and queued prompts apply routing inside queued-input promotion after the input is claimed.
- Explicit reviewer/integrator reruns reject requests while the role child is pending/queued/running or has an active execution. Accepted reruns reactivate the parent and child, move completed role children back to Active, set pending before submission, and invalidate freshness.
- Reviewer and worker completion auto-submit downstream integrator/reviewer children as active/pending so the worker queue cannot prune them.
- Deferred swarm creation is supported through the `Autonomous planner` UI toggle and chat runtime `start_immediately:false`; omitted/default values still start immediately. Visible generic `/tasks/:id/run` detects swarm parents and starts/resumes the planner instead of submitting the parent as an ordinary task.
- Slack, Telegram, Discord, and Email channel runtimes handle advertised `create_swarm_task` through shared channel action handling, create real swarm parents with channel origin/context metadata, and use the active authorized channel project rather than caller-supplied `project_id`.
- Multiple swarm workers assigned the same bug-fix objective can independently converge on the same production fix with different local worker branches and varying test-coverage completeness; a reviewer pass should audit each worker's actual branch diff (not just its self-reported summary), accept the single best/most-complete overlapping branch for integration, and reject the redundant ones rather than merging duplicates. Observed 2026-07-02 on an Email `switch_project` authorization bug swarm with 5 overlapping worker branches, and again 2026-07-03 on a second "Swarm Test 2" run (planner `92d2578d92f3fc3df0e0f58491ef6366`, parent `fde3b9fe6ca4c49296298cb871d411ae`): the planner split the same underlying bug into 3 differently-worded worker angles (repo normalization, runtime `switch_project` wiring, saved-project fallback), but all 3 workers independently patched the same root cause in `repository.NormalizeEmailAddress` with near-identical production hunks and only differing test coverage; the reviewer correctly picked the branch with the broadest test coverage and rejected the others as redundant. In that same run, the integrator child was `cancelled` (`task cancelled by user`) which left the parent stuck `blocked`/`needs_integration` even though planner/workers/reviewer all completed — a swarm can look "stuck" purely because a human cancelled the integrator step, not because of an orchestration bug.
- Child `swarm_status` (worker/reviewer/integrator) is API-visible (`GET /api/tasks/:id/swarm`) and used as durable coordination metadata, not purely cosmetic; completed children previously retained non-terminal labels like `running`/`ready` indefinitely because `OnChildCompleted` wrote back the pre-completion `child.SwarmStatus` instead of a terminal value. Fixed 2026-07-02 (commit `6db0aa8`, `Terminalize swarm child status on completion`): worker completion now sets `swarm_status="completed"`, reviewer completion sets `"reviewed"`, integrator completion sets `"integrated"`, and both initial and follow-up planner success set the planner child to `"planned"`. `TestSwarmServiceTerminalizesChildSwarmStatusOnCompletion` in `internal/service/swarm_service_test.go` covers the full planner→worker→reviewer→integrator terminal-status chain.
- That same terminalization work surfaced and fixed a real orchestration bug: the reviewer auto-submit block in `OnChildCompleted` ran on every call where `allRequiredWorkersCompleted(...) && parentCfg.ReviewerEnabled` was true, including when `OnChildCompleted` was invoked for the reviewer's own completion — so a completing reviewer could reactivate/resubmit itself. Fixed by gating that block to `child.SwarmRole == models.SwarmRoleWorker && child.Status == models.StatusCompleted && ...` so only worker completions auto-start the reviewer. The integrator finalization path was also fixed to reload `children` and the integrator row from the repo (not a stale in-memory snapshot) immediately before evaluating the final integration-freshness guard.
- Fixed 2026-07-02 (commit `d0149d7`, `Start integrator when reviewer is disabled`): `ReviewerEnabled=false` + `IntegratorEnabled=true` previously stranded the parent because `OnChildCompleted`'s integrator auto-start block only computed its target generation from an existing reviewer child's `ReviewedGeneration`. Fixed by computing `integrationTargetGeneration` as `parentCfg.Generation` (once all required workers for that generation are completed) when `!parentCfg.ReviewerEnabled`, so review-disabled swarms auto-start the integrator directly from worker completion. Regression: `TestSwarmServiceStartsIntegratorAfterWorkersWhenReviewerDisabled` in `internal/service/swarm_service_test.go`.
- Fixed 2026-07-02 (commit `da8244e`, `Complete swarms without integrator`): `IntegratorEnabled=false` (independent of reviewer state — reviewer-enabled/no-integrator or no-reviewer/no-integrator) previously stranded the parent in a non-terminal state because `RecomputeParentStatus` only transitioned to `StatusCompleted` via a completed-integrator-child branch. Fix added a `swarmCompleteWithoutIntegrator(children, parentCfg)` helper/branch in `RecomputeParentStatus` (internal/service/swarm_service.go) that completes the parent once `allRequiredWorkersCompleted` for the current generation and, if `ReviewerEnabled`, the reviewer has completed with `ReviewedGeneration >= parentCfg.Generation`; it marks parent `swarm_status="current"`, sets `IntegratedGeneration = Generation`, and moves category to Completed, without fabricating a parent execution/result (existing parent output is left untouched). Regressions: `TestSwarmServiceCompletesReviewerOnlySwarmWithoutIntegrator` and `TestSwarmServiceCompletesWorkerOnlySwarmWithoutReviewerOrIntegrator` in `internal/service/swarm_service_test.go`.
- Users have found the `reviewer`/`integrator`/`planner`/`worker` swarm role names unclear (one user confused `integrator` with `interrogator`); this was user feedback that has since been acted on (see rename below).
- User explicitly requested 2026-07-04 (on task 11090658): rename the `integrator` swarm role to `merger` throughout the product (code, prompts, UI, docs). This was implemented later the same day (commit `8ae7dd2`, `Rename swarm integrator to merger`), discovered and completed mid-turn during an unrelated card-styling request after the worktree was found with interrupted/partial uncommitted rename artifacts (including a compile error) from an earlier failed turn. The rename is now live: new/canonical code uses `models.SwarmRoleMerger` (`"merger"`), `MergerEnabled`, `MergerPrompt`, `MergedGeneration`, and UI/docs say "Merger"/"Merge". Legacy compatibility is intentionally preserved rather than migrated: `models.SwarmRoleLegacyIntegrator` (`"integrator"`) is still a recognized `swarm_role` value everywhere queries/list/lookup happen (`ListSwarmChildren`, `FindSwarmChildByRole`), `SwarmConfig.NormalizeMergerFields()` folds legacy `integrator_enabled`/`integrator_prompt`/`integrated_generation` JSON into the canonical merger fields on read and write, the API route `/api/tasks/:id/swarm/rerun-integrator` remains registered as an alias to the same handler as `/api/tasks/:id/swarm/rerun-merger`, and the task-edit form still accepts legacy `swarm_integrator_enabled` in addition to `swarm_merger_enabled`. `SwarmRoleDisplayName` maps both `SwarmRoleMerger` and `SwarmRoleLegacyIntegrator` to "Merger" for display. No data migration was needed or performed; existing persisted swarm rows with `swarm_role='integrator'` keep working.
- Known gap identified 2026-07-03: `reviewerPrompt()`/`mergerPrompt()` (renamed from `integratorPrompt()`, see rename above) in `internal/service/swarm_service.go` do not inline an explicit worker-task-ID manifest (IDs/titles/statuses) into the reviewer/merger prompt body. The reviewer prompt asks the model to return accepted/rejected "worker task ids" but the model must discover those IDs itself via task-thread tools (`view_task_thread`) or ambient task context/git-branch discovery rather than being given a generated list. This question was re-investigated across many reactivated continuation turns on task 11090658 without ever reaching a definitive materiality fix-or-skip decision. Resolved 2026-07-04: on a later turn's full-scope audit, this was explicitly evaluated against the checked-in runbook and found to have no runbook requirement for an inline manifest, and roles with git/shell access already work around it via branch enumeration (see below); it was classified as a non-blocking reliability improvement, not a material defect, and the task goal's audit-only review passed without requiring a fix for it. Still worth revisiting as a future UX/reliability improvement, especially for channel-origin or non-git-capable swarm roles.
- Direct evidence gathered 2026-07-03 by querying the live app DB (`~/.openvibely/openvibely.db`) for swarm parent `fde3b9fe6ca4c49296298cb871d411ae` (planner `92d2578d92f3fc3df0e0f58491ef6366`, the "Swarm Test 2" run): neither the reviewer (`2dc7a3a749006f417c03aea8e6ac66e6`) nor the integrator (`578e5c665839d18145ac9b6ac450b6d6`) execution actually called `view_task_thread` at all — grepping their stored `executions.output` tool-call markers found zero `view_task_thread` invocations for either role. Instead, both roles discovered and inspected worker changes directly via git: the reviewer ran `git worktree list`, `git branch --list 'task/*'`, `git show <worker-branch>:<file>`, and `git diff main...<worker-branch> -- <files>` against each worker's task-scoped branch (`task/<id>-<slug>`), and the integrator worked directly in a checked-out worktree using file/edit/build/test tools. Worker task IDs appeared in reviewer/integrator prompt text nowhere (`instr(prompt_sent, '<worker_id>')` = 0 for all three worker IDs on both roles); the IDs the reviewer output referenced were derived from discovered git branch names/commits, not from a manifest in the prompt. This confirms in practice (not just in theory) that reviewer/integrator rely on git-branch/worktree discovery rather than `view_task_thread` or an inlined worker-ID manifest, and that this particular run's reviewer succeeded despite the gap while the integrator was separately cancelled by the user (unrelated to this gap).
- Follow-up finding 2026-07-03 on that same cancelled integrator (`578e5c665839d18145ac9b6ac450b6d6`, swarm parent `fde3b9fe6ca4c49296298cb871d411ae`): before being cancelled, it had already made real file edits directly in its own dedicated integrator worktree (`.worktrees/task_578e5c665839d18145ac9b6ac450b6d6`, branch `task/578e5c66-swarm-test-2-integrator`) to `internal/repository/email_auth_repo.go`, `internal/repository/email_auth_repo_test.go`, `internal/repository/email_sender_project_repo_test.go`, and `internal/service/email_service_test.go` — consistent with its prompt instruction to apply accepted worker changes into the parent integration worktree rather than using a worker worktree as the final destination. Those edits were left uncommitted when cancelled and diverge somewhat from the accepted worker branch (`task/c6fa0004-email-inbound-saved-project-fallback-worker`): diffstat `+106/-11` across 4 files in the integrator's uncommitted worktree state vs `+151/-5` on the accepted worker branch, i.e. the integrator had started applying/adapting the fix but had not finished reproducing the full accepted worker diff when it was cancelled. Separately confirmed: inspecting/diagnosing another task's swarm worktree (via its DB-recorded `worktree_path`) is fully isolated from and does not affect the current task's own assigned worktree — each task/swarm role has its own independent worktree and branch.
- Confirmed 2026-07-04 (still no code fix landed): for swarm parent `fde3b9fe6ca4c49296298cb871d411ae`, the cancelled integrator (`578e5c665839d18145ac9b6ac450b6d6`) effectively rewrote/reimplemented the reviewer-accepted worker fix from scratch in its own integrator worktree rather than checking out and applying the accepted worker branch's diff — its prompt did not include the accepted worker branch/ID, so it traced the bug independently and recreated a similar-but-diverging patch. This confirms the integrator prompt design is too implicit: the recommended fix (not yet implemented) is to explicitly inject the reviewer's accepted worker branch name/ID into the integrator prompt and instruct it to inspect/apply that branch's diff directly (e.g. `git diff main...<accepted-branch>`), only reimplementing by hand if the diff cannot be applied cleanly.
- The task/agent swarm feature (planner/worker/reviewer/merger, formerly "integrator" — see rename above) reached a stable, fully-implemented state through task 11090658: all four reviewer/merger enabled/disabled lifecycle combinations (both enabled, reviewer-only, merger-only, worker-only) reach a terminal completed parent state, migrations run through `107_swarm_tasks.go`, and repeated independent full-scope audit-only reviews (through 2026-07-04, at final branch HEAD `a691a8b` after the merger rename and swarm-card accordion styling work) found no material bugs, regressions, or missing requirements. Final task-completing commits: `8ae7dd2` (`Rename swarm integrator to merger`) and `a691a8b` (`Polish swarm card accordion styling`, converted the swarm task-card child list from a static block into a real `<details>/<summary>` accordion and mapped internal statuses like `needs_integration`/`integrated` to display labels like "Needs merge"/"Merged" via a new `SwarmStatusDisplayName` helper). A later same-day continuation thread on task 11090658 spent many turns answering plain user Q&A about swarm mechanics (how reviewer/integrator discover worker IDs, whether `view_task_thread` was actually used on the `fde3b9fe6ca4c49296298cb871d411ae` run, how the merger applies accepted fixes, what happens when the reviewer rejects a worker, and general planner/worker/reviewer/merger/parent architecture explanations) before the user asked for another audit-only review; that final fresh, full-scope, strictly read-only audit at the same clean commit `a691a8b` re-confirmed no material bugs, regressions, or missing requirements across swarm lifecycle, merger rename compatibility, UI accordion/status rendering, channel scoping, and migrations, so the task goal remained satisfied without further code changes.
- Design gap explained 2026-07-04 (not yet verified against source or fixed): when the reviewer rejects one or more worker outputs, enforcement that the merger only uses accepted workers is soft/prompt-based rather than mechanically enforced — there is no structured reviewer accepted/rejected-worker-ID contract stored and passed to the merger, and no hard merge gate that restricts the merger to only accepted worker branches/diffs. The merger is expected to follow the reviewer's stated acceptance/rejection from prompt/context, but nothing in the orchestration code prevents it from drawing on a rejected worker's changes if prompt/context is ambiguous. This is consistent with the already-documented worker-ID-manifest and merger-reimplementation-vs-apply gaps; a more robust design would have the reviewer emit structured `accepted_worker_ids`/`rejected_worker_ids` (with reasons) that the system stores and then injects only the accepted branch(es) into the merger prompt.
- Recurring audit-methodology lesson from that task's fix cycle: under a strict "audit finds one issue, stop and fix separately" workflow on a large, actively-fixed feature area, expect several rounds where each fresh full-scope audit turns up exactly one new bug in an adjacent/analogous code path before a clean audit is reached — don't assume near-completion after only one or two clean-seeming rounds, but also don't assume the cycle never terminates.
- Pending as of 2026-07-04 on task 11090658 (not yet implemented): after the `a691a8b` accordion styling landed, the user requested two more swarm task-card UI fixes — (1) the `<details>/<summary>` accordion container is too narrow relative to the surrounding task container card (per user screenshot) and needs a width fix, and (2) the accordion should be collapsed by default rather than open/expanded. The turn that received this request failed before making any changes (transient provider overload error, no code touched), so this remains open/unimplemented; treat the earlier "fully complete/closed" status for this feature as superseded until these two fixes land and a fresh audit-only review is done.
- Plan handoff is driven by completed Plan-mode responses containing `<proposed_plan>`.
- Task-thread follow-ups use chronological execution history rather than reinjecting the original task prompt.
- Task-thread follow-up HTTP send/acceptance should stay lightweight and must not synchronously load/render full execution history, rebuild full transcripts, resolve worktrees, or build model-only context before responding; model-call setup runs in the background after the user message/execution is durably accepted.
- Task-thread model replay history is bounded to the provider context window: background follow-up, queued-promotion, and failed-followup retry paths query only enough latest task executions to preserve up to 20 prior replay turns after filtering the current execution.
- Deferred task-thread setup failures after durable acceptance should still run normal terminal/finalization cleanup that can promote the next queued input.
- The Task Thread transcript/pagination renders `executions` history; `tasks.prompt` may not appear as the oldest paged thread item unless also recorded as an execution prompt.
- Task-thread follow-ups run normal task lifecycle routing before the follow-up model call; selected skill/memory semantics are canonical in lifecycle and managed-memory memories.
- Interactive Chat uses narrower recall-only memory preparation rather than full Skill Curator routing.
- Lifecycle-agent activity belongs in the task Lifecycle tab rather than the main Thread tab.
- Task goals are durable `task_goals` records managed by `TaskGoalService`. Chat orchestration supports explicit and implicit task-goal creation.
- Goal continuation uses normal task-thread follow-ups through `thread_inputs`; it does not start work inline.
- Tasks do not support direct peer-to-peer chat. Coordination is mediated through app state and control-plane mechanisms such as `send_to_task`, `view_task_thread`, chain-created child tasks, schedules, dynamic wakeups, persisted goals, and durable `thread_inputs`.
- A durable “inbox task” pattern is a canonical task thread, not an automatic mailbox.
- Goal status writes use stale `goal_id` plus active-status guards. Goal status tools can be granted to agents beyond Goal Agent, but ungranted/default agents do not see or execute them.
- Manual/user follow-ups on achieved goals reactivate the same goal before prompt context and lifecycle evaluation; Goal Agent/system-agent continuations do not reopen achieved goals.
- Manual stop/cancel signals implicitly pause the active task goal with reason `stopped by user`, preserving the goal for intentional resume. Starting a task again resumes only goals paused with that reason; explicit/manual goal pauses remain explicit-resume-only.
- Global Chat stop uses the active Chat execution’s backing task and reuses task cancellation semantics while preserving `category=chat` so Chat history remains visible.
- `processStreamingResponse` must re-check persisted task/execution cancellation after registering runtime cancellation and before model/provider preparation, and check the runtime context immediately before provider dispatch to avoid early-cancel races.
- Task Thread composer Stop posts to the task cancel path with composer-stop semantics so it cancels the active run/capacity-wait without bulk-cancelling queued follow-up rows. Broader task cancel controls may still cancel pending thread inputs.
- Streaming task-thread cancellation finalization for normal tasks must not leave `tasks.category='active'` with terminal `failed`/`cancelled` status; normal active terminal rows are normalized to Backlog, while Chat tasks preserve `category=chat` so Chat history remains visible.
- Direct task-thread cancel and Active-to-Backlog/Completed moves cancel queued/running execution, pause the active goal, and preserve the user's requested target category. `TaskService.UpdateCategory` must use the pre-update category/status for stop semantics.
- Goal Agent `send_to_task` re-checks active goal state and execution freshness before queueing continuations, preventing stale lifecycle hooks from restarting stopped or superseded work.

Task execution and scheduling facts:
- Active tasks auto-submit to the worker pool on creation or when moved to Active.
- Scheduled tasks run when `next_run <= now`; one-time schedules clear `next_run`, while repeating schedules compute the next occurrence.
- Schedules have durable enabled/disabled state. Disabled schedules are excluded from due-run selection until re-enabled; toggling off preserves `next_run`, while toggling on recomputes stale/past `next_run` to the next valid future occurrence.
- Dynamic task-loop wakeups are schedule rows with wakeup metadata and replacement semantics. When due, the scheduler enqueues normal task-thread follow-ups through durable `thread_inputs`; wakeups are blocked for stopped goal states and failures should be visible on the task timeline/event stream.
- Chat control-plane schedule modification supports `enabled: bool` and must stay consistent with HTTP/API toggling semantics.
- Promoted queued task-thread follow-ups move the task back to Active during the queue-claim/promotion transaction before the follow-up execution is exposed.
- Reactivating a failed task with no queued input should retry the latest failed follow-up using its `PromptSent` and chronological history, not blindly resubmit `tasks.prompt`; older failed follow-ups are ignored if a later execution succeeded.
- Zero-model guardrails block task execution and Chat send surfaces with an `Open Models` action.
- Channel-origin runs follow the same queueing, steering, and task-thread rules where supported.
- Worker dispatch capacity uses global/project/model counters. Cleanup after slot acquisition is centralized so completion, provider errors, cancellation, claim failures, and provider panics release slots, clear pending/cancel state, and trigger redispatch.
- Task-thread follow-up slot waiting is not charged against `worker_timeout`; follow-ups wait for project/model slots with a cancellable no-deadline queue context, then start turn-processing timeout after slots are acquired. Capacity-waiting follow-ups are cancellable while `queued`.
- Tasks legitimately run/dispatch while `category` is either `active` or `scheduled` (matches `worker_service.go` dispatch/prune logic). `RecoverStaleRunningTaskExecutions` (`internal/repository/execution_repo.go`) previously used `t.category != 'active'` to decide an execution's owning task was inactive/stale; since this sweep runs on nearly every `FindActiveTaskExecution`/`HasActiveTaskExecution` call (not just startup), it incorrectly flipped live running executions of legitimately-running `scheduled`-category tasks to `failed` with error `"Recovered stale running execution: owning task is terminal or inactive"`, while the real worker goroutine kept running obliviously and `tasks.status` stayed `running`. Fixed (2026-07-02) to `t.category NOT IN ('active', 'scheduled')`. This same corrupted execution state (exec `failed` while task stays `running`) was also the root cause of task-thread scroll thrashing: see `realtime_and_frontend_patterns.md`.

