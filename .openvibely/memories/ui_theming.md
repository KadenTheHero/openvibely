---
name: ui_theming
type: project
created: 2026-08-11
updated: 2026-08-11
source: update_memory
source_id: d87ef6d051970bf69bec289136a3a796:ca72e8e78bde6afd
confidence: high
title: UI Theming
---

OpenVibely has a built-in theme system for application presentation preferences, including native OpenVibely defaults and imported VS Code palettes.

Durable theming contracts:
- `/themes` is the dedicated Themes page and is linked from the main sidebar navigation. It supports both full-page and HTMX fragment rendering, and project switching may preserve `project_id` only for shell consistency; the selected theme is browser/application state, not project configuration.
- The selectable catalog prepends two native first-party defaults, `OpenVibely Dark` (`openvibely-dark`) and `OpenVibely Light` (`openvibely-light`), before the imported VS Code themes. These are the bundled default/fallback theme IDs and should restore the original pre-VS-Code-theme OpenVibely palettes and styling path, not approximate VS Code/DaisyUI replacements: Dark uses `#191E24` for the page/content canvas, `#1D232A` for sidebar/cards/modals/inputs, and `#15191E` for the original dark base border; Light uses `#F5F5F5` for the page/content canvas and `#FAFAFA` for sidebar/cards/modals/inputs.
- Built-in VS Code themes are imported offline from the open-source `microsoft/vscode` repository pinned at tag `1.130.0`. The reproducible update command is `go run ./internal/themes/cmd/generate`.
- The generated VS Code catalog lives under `internal/themes` and includes all 19 first-party color themes contributed by pinned built-in `theme-*` extensions: Abyss, Dark Modern, Light Modern, Dark (Visual Studio), Dark 2026, Dark High Contrast, Dark+, Kimbie Dark, Light (Visual Studio), Light 2026, Light High Contrast, Light+, Monokai, Monokai Dimmed, Quiet Light, Red, Solarized Dark, Solarized Light, and Tomorrow Night Blue. The full selectable catalog is 21 themes when the two OpenVibely defaults are included.
- Theme JSON includes/inheritance are resolved at generation time. Generated runtime values are sanitized normalized OpenVibely semantic colors, syntax colors, and preview colors; the app must not fetch VS Code/GitHub/Marketplace data at runtime.
- DOM state separates compatibility mode from exact palette: keep `data-theme="light"` or `data-theme="dark"` for DaisyUI/existing selectors and use `data-color-theme="<stable-theme-id>"` for the exact selected palette. High-contrast themes retain high-contrast identity even when their base compatibility mode is light or dark.
- Exact selection is persisted locally separately from legacy `localStorage.theme`. Legacy `localStorage.theme` values of `light` or `dark` are migrated/fallbacked to the OpenVibely Light/Dark defaults; invalid exact IDs safely fall back to those defaults.
- The footer sun/moon control is a quick mode switch that toggles between the most recently selected light-side and dark-side themes, treating high-contrast-light as light and high-contrast-dark as dark.
- Footer theme-mode controls can render after the early head script, so runtime theme application must export/rerun a control sync after DOM insertion. Initial sidebar markup should avoid hardcoded dark-mode ARIA/tooltips; `aria-pressed`, `aria-label`, `title`, and tooltip text derive from the current selected light/dark mode.
- Apply theme CSS variables early before paint to avoid theme flash. To avoid response-size regressions, base layout embeds only a compact runtime catalog needed for early application; the full catalog remains available server-side for the Themes page.
- Application and content backgrounds for imported VS Code themes must follow the selected exact VS Code palette, not DaisyUI/default fallbacks. Semantic `contentBg` should prefer VS Code `editor.background`/`window.background` before secondary panel/sidebar surfaces, and app-shell CSS should apply the generated content background to the root/page canvas, `.drawer-content`, and `#main-content`; the Red theme regression is that non-nav content must use its red editor background (`#390000`) instead of generic dark gray (`#252526`).
- Modal surfaces for imported VS Code themes are part of the exact-theme contract: `.modal-box`, nested modal base surfaces/action strips, close-button hover/focus states, and modal backdrop treatment should derive from generated OpenVibely semantic variables such as `--ov-surface-raised`, `--ov-text`, `--ov-border`, `--ov-hover-bg`, and `--ov-focus`, rather than DaisyUI/default light/dark backgrounds.
- Card surfaces and borders for imported VS Code themes are part of the exact-theme contract: `.card`, searchable cards, dropdown card actions, `border-base-300` variants, divider utilities, directional border utilities, and card hover states such as `hover:border-primary*` should resolve to generated border roles like `--ov-border` and `--ov-border-strong`, not DaisyUI base/primary border fallbacks.
- Schedule and task-board interaction surfaces for imported VS Code themes are part of the exact-theme contract: schedule grid/timeline accents, today highlights, scheduled/disabled task chips, legend swatches, schedule drag valid/invalid feedback, kanban `.drag-over`, task selected states, category/task dropzones, and legacy shared link/accent tokens such as `--ov-link-color` should consume generated semantic variables such as `--ov-border`, `--ov-link`, `--ov-selection-bg`, `--ov-focus`, `--ov-hover-bg`, and `--ov-diff-delete-bg`, rather than fixed indigo/red/DaisyUI colors.
- Generated semantic variables should be visibly consumed by imported VS Code theme CSS, including status feedback (`info`, `success`, `warning`, `error`), selection, hover, focus, input borders, card borders, modal surfaces, schedule/dropzone interaction states, and diff/review add/modify/delete backgrounds, so UI states follow the exact selected VS Code palette rather than fixed DaisyUI light/dark colors. Native `openvibely-*` defaults should not be caught by the invasive VS Code layout/surface/border override selectors; they should keep the original OpenVibely default nav, card, border, schedule, dropzone, and modal styling behavior.
- Chat and task-thread surfaces for imported VS Code themes are exact-theme UI: user bubbles should use generated button/accent roles, assistant bubbles and chat input should use raised surface/border/text roles, embedded chat code/tool panels should use generated code/surface/border/text roles, and stream tool toggles/status icons should use generated link/focus/success/error roles rather than DaisyUI light/dark fallbacks. Keep these selectors scoped to `[data-color-theme^="vscode-"]` so native OpenVibely defaults preserve their original chat-bubble/input styling path.
- Button variants for imported VS Code themes are exact-theme UI: primary/accent, secondary/neutral, info/success/warning/error, ghost, link, outline, hover, and focus states should derive from generated semantic variables such as `--ov-button-bg`, `--ov-button-hover-bg`, `--ov-button-secondary-bg`, `--ov-info`, `--ov-success`, `--ov-warning`, `--ov-error`, foreground contrast helpers, `--ov-hover-bg`, `--ov-focus`, and `--ov-selection-bg`, not fixed DaisyUI/native light/dark button colors. Keep the CSS compact rather than large repeated selector matrices because the base layout and downstream pages are size-gated.
- The theme generator and attribution docs should describe semantic fallback/derivation rules for missing VS Code colors: ordered upstream color keys win first, transparent or invalid values are ignored/sanitized, missing roles derive through theme-aware mixes/alpha/best-text helpers, and remaining gaps fall back to bundled light/dark defaults.
- Highlight.js should not load a fixed GitHub Dark stylesheet. Rendered Markdown/code block syntax colors derive from the selected theme through generated CSS variables and must work for initial and HTMX-inserted content.
- For imported VS Code themes, the Automation YAML editor and read-only Live YAML panel are exact-theme code surfaces: panel background/foreground, gutter, line numbers, highlight overlay, caret, YAML keys/values/scalars/comments, diagnostics, indentation dots, and indentation rails should use generated code/syntax/border/focus/error variables such as `--ov-code-*`, `--ov-syntax-*`, `--ov-border-*`, `--ov-focus`, and `--ov-error`, with only safe fallbacks for standalone browser fixtures.
