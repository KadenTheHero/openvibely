---
name: testing_coverage_and_performance
type: project
created: 2026-06-07
updated: 2026-08-02
source: task_turn
source_id: 663ac92f373501a5ab861457513a5b18:c2d29179939a5139
confidence: high
title: Testing Coverage and Performance
---

OpenVibely has a large Go test suite whose measured coverage is limited more by subsystem breadth gaps and generated templ output than by test count.

Coverage direction:
- `internal/service` and `internal/handler` remain the largest coverage gaps, especially error, pagination, webhook, retry, and large service/handler paths.
- Generated templ `*_templ.go` files are excluded from coverage summaries while template tests still run.
- The highest-value remaining target is `service/llm_service.go`; it needs controlled LLM caller mocks to avoid flaky tests. Existing workflow, Telegram, and provider-adapter suites should broaden beyond happy paths.
- Existing tests are generally useful; prefer broader behavioral coverage over adding more granular tests to already-covered code. Avoid blanket `t.Parallel()` around shared database setup.
- A shared handler `TestMain` database is unsuitable because many tests mutate global/default/list state. `NewTestDB` uses an immutable serialized SQLite template to create fresh isolated in-memory fixtures; safe sharing of one mutable database would still require transaction rollback isolation.

Current performance seams:
- Task deletion performance is guarded by migration-130 and migration-132 query-plan tests plus a representative concurrency fixture. Migration 130 indexes task/execution foreign keys and cleanup ownership lookups, including `alerts.execution_id`; migration 132 indexes the self-referencing `lifecycle_executions.parent_execution_id` foreign key. Without the lifecycle parent index, deleting each lifecycle row scanned the entire lifecycle table, so production-shaped histories caused quadratic cascade work: a live deletion held the sole SQLite connection for 24.86s, and a deterministic fixture took 13.09s while blocking an unrelated query for the same duration. The fixture now covers 2,000 executions, 2,000 lifecycle runs, 20,000 lifecycle events, 50,000 unrelated lifecycle runs and alerts, 500 queued thread inputs, 100 attachment rows, schedules, goals, analytics, retained alert references, and swarm relationships; with both index migrations, deletion and unrelated-query delay are approximately 127ms and 126ms. Keep query-plan assertions for indexed `SEARCH` operations, especially `alerts.execution_id`, `lifecycle_executions.parent_execution_id`, and cleanup-ownership lookups, because the single-connection SQLite pool turns scans into server-wide stalls. Service regressions cover single and completed/backlog/chat bulk cleanup of finalized task attachments, execution/chat attachments, and pending-upload sessions; concurrent upload publication at the cancellation boundary; unsafe session IDs and missing upload-root rejection before cancellation/deletion; durable-delete failure preservation; surfaced post-delete cleanup failures; shared-reference retention; and managed-worktree preservation.
- Attachment cleanup coverage should retain production-shaped Clear Chat deletion, queued/immediate publication rollback, direct Chat/task-thread failures, shared-reference preservation, durable session retirement, and per-session publication fencing. Deterministic concurrency tests must cover upload-first and retirement-first ordering; query-plan coverage must require indexed task, run-execution, and shared-session lookups with no `thread_inputs` scan.
- Open issues track inefficient queued-input recovery indexing (`#22`), managed-worktree diff snapshots (`#31`), task-thread polling projections (`#39`), worker queue dispatch scans (`#42`), per-execution SSE catch-up reads (`#46`), GitHub PR-feedback forwarding (`#53`), assigned-issue PR lookups (`#58`), idle scheduler logging (`#63`), due-schedule task lookup fan-out (`#70`), redundant CI compilation (`#73`), Automation portfolio query fan-out (`#74`), Channels page settings-read fan-out (`#80`, approximately 50 `app_settings` queries per render), agent-plugin marketplace discovery (`#90`, three manifest reads/JSON decodes per valid marketplace in each `DiscoverState` call), synchronous lifecycle trace transaction fan-out (`#108`), repeated skill-analytics catalog discovery (`#117`), Alerts page project-filtered newest-first sorting (`#127`), redundant hardened GitHub runtime construction during Automation-bound task follow-ups (`#137`), unbounded History/Reflection execution-list payloads (`#151`), autonomous-build dashboard summary query/materialization fan-out (`#164`), oversized `list_tasks` summary projections (`#166`), eager Models page configuration projection/rendering (`#179`), Insights dashboard query/materialization fan-out (`#180`), oversized runtime `list_alerts` discovery projections (`#193`), and oversized `GET /workflows` discovery projections (`#199`). Verify issue state before acting.
- `NewTestDB` runs migrations and seed SQL once, serializes an immutable SQLite template, and deserializes it into each fresh isolated in-memory fixture. A measured implementation reduced warm create-and-close latency from approximately 12.7ms to 1.7ms, allocated bytes from 9,852 to 3,208 B/op, allocations from 480 to 69, and the uncached internal-suite median from 51.4s to 42.4s. Tests must preserve `_loc=UTC`, `foreign_keys=ON`, `busy_timeout=5000`, `MaxOpenConns(1)`, migrated schema and seed data, cleanup, default-agent behavior, and simultaneous-fixture isolation. `BenchmarkNewTestDB` is the regression benchmark.
- Agent-plugin `DiscoverState` has a measured performance seam: each valid marketplace manifest is read and parsed three times during directory validation, state augmentation, and the unconditional default-seeding check. Issue `#90` targets reducing this from `3N` reads/decodes to at most `N` per call while preserving invalid-manifest handling, ordering, installed-cache behavior, and default seeding; validate with I/O-count instrumentation and representative latency/allocation benchmarks.
- Lifecycle event persistence is synchronous on the provider execution goroutine and uses the process-wide single SQLite connection. A successful lifecycle LLM hook performs at least `7 + 2T` serialized trace transactions for `T` runtime tool calls, repeatedly assigning indexed sequence numbers and committing each event independently. Issue `#108` targets constant foreground database waits and at least 50% lower median trace overhead for a 50-tool fixture, with transaction-count, latency, allocation, and contention benchmarks while preserving trace durability and ordering.
- The skill analytics dashboard repeats catalog metadata discovery on every initial load, refresh, date-range change, and grouping change, even when catalog data is unchanged. The path rereads standalone indexes for `always_use`, stats each indexed skill, reads and YAML-parses every agent-owned `SKILL.md`, performs project-root database lookups, and then serially runs five aggregate queries on the single SQLite connection. Issue `#117` targets warm-request caching with supported mutation invalidation; validate via filesystem I/O counts and handler latency/allocation benchmarks that distinguish cold from unchanged warm requests while preserving catalog freshness and project scoping.
- Migration 135 implements issue `#127` by replacing the project-only Alerts index with `idx_alerts_project_created` on `(project_id, created_at DESC, id DESC)`. Because `project_id` is the leading column, the composite index still serves project-equality lookups while matching stable newest-first order. A production-shaped 300,000-row fixture reduced median newest-100 `ListFiltered` latency from 4.386ms to 0.2425ms without changing allocation or insert behavior; preserve query-plan, ordering, pagination, project-scoping, filter, and metadata-decoding coverage.
- Issue `#132` uses a Kanban-specific projection that truncates prompts to 300 Unicode code points in SQLite before Go materialization. Full task detail/edit and general task-list paths retain complete prompts. Preserve Unicode, sorting, card metadata, swarm grouping, omission-of-tail, latency, allocation, and response-size coverage.
- Issue `#147` uses a lazily rebuilt immutable subscriber snapshot per execution group in `ExecutionStreamHub`: membership changes mark it dirty, the next publish rebuilds it under lock, and steady-state publishes reuse it. Preserve zero-allocation steady-state publishing and concurrent Publish/Unsubscribe/Close coverage.
- The active History/Reflection workflow calls `ListRecentExecutions` without a row limit and selects full `prompt_sent`, `output`, and `error_message` values even though History and Reflection display only bounded previews. A representative 500-row fixture materializes at least 46.875 MiB of prompt/output text while History displays at most about 146.5 KiB. Issue `#151` targets bounded repository projections plus a full-handler benchmark and an 80% allocation reduction while preserving ordering, summaries, status metadata, preview behavior, and detail navigation; the unmounted legacy task-detail execution-history fragment is outside this path.
- The autonomous-build dashboard first loads every project task to retain at most 10 roots, then each completed root independently reloads both the root and the entire project task list before execution-output lookup. For `C` completed roots it performs `1 + 2C` task queries and up to `C` execution-list queries through the single SQLite connection; at `C=10`, this is up to 31 serialized queries, including 11 full-project task materializations with complete prompts. Issue `#164` targets batched summary assembly with a production-shaped fixture, query-count instrumentation, latency/allocation thresholds, and contention measurement while preserving dashboard behavior.
- The `list_tasks` task-discovery workflow returns only 8 summary fields but its repository query selects and materializes all 30 task columns, including unbounded prompts and chain/swarm/worktree/lineage data. A valid 50-row page containing 1 MiB prompts causes at least 50 MiB of prompt data to pass through SQLite and Go before the unused fields are discarded; existing tests use one-byte prompts and do not measure allocations or latency. Issue `#166` targets a compact projection, production-shaped benchmarks, and at least 90% lower allocations while preserving filtering, ordering, pagination, and project isolation.
- Issue `#173` remains active pending a fresh separate strictly read-only audit after the stale-precedence fix in task-branch commit `5361b95f` on 2026-08-02. Root scans now track active and displaced declaration keys, prune cached paths absent from the current directory even when the whole project `agents` directory disappears, and reapply matching unchanged cached global declarations only when the active project no longer owns those keys. Regression coverage proves project declaration removal and re-keying restore the old key to global scope without rereading unchanged global content, and the full HTMX `ListAgents` path does not rematerialize the deleted project declaration. Focused race tests, `go build ./...`, and `TMPDIR=/private/tmp go test ./internal/... -count=1 -timeout 120s` passed. Ten benchmark samples retained zero warm declaration reads/parses, zero SQLite writes, and one full agent list; median handler latency improved from 41.98ms to 3.32ms, B/op from 37.06MB to 14.45MB, allocations from 205,094 to 22,732, unrelated-query median wait from 588.9µs to 1.81µs, and p95 from 2.244ms to 112.5µs. The corrected baseline remains faithful with exactly 102 historical hook lookups for 102 rendered agents. PR publication through `github_open_pull_request` failed with `task not found in current project`, so verify branch/PR state separately.
- Issue `#179` is implemented and passed the required fresh separate strictly read-only audit at task-branch commit `e7364c61` (feature commit `78ec45bc`) on 2026-08-02. The implementation uses one compact Models card query, omits secret/edit-only and unbounded configuration bodies from initial rendering, fetches one full record on edit, and guards stale edit-detail responses. The corrected benchmark sends both the historical eager baseline and lazy candidate through Echo `GET /models` routes via `ServeHTTP`, with 50 OpenAI-compatible configs containing exactly 1 MiB `extra_body_json` each. Ten repeated samples measured median latency from 104.450ms to 0.281ms (99.73% lower), B/op from 377.310MB to 0.863MB (99.77% lower), and initial response size from 52.685MB to 0.250MB (99.53% lower); `benchstat` was unavailable. The server build, focused repository/handler/template tests, and full `internal/...` suite passed on 2026-08-02. The audit found no material bugs, regressions, or missing requirements and left the workspace unchanged. Updating the existing PR through `github_open_pull_request` was blocked by Automation-graph authorization, so verify branch/PR publication state separately.
- The `/insights` handler launches 12 repository operations concurrently, but the single-connection SQLite pool serializes them. It also loads up to 1,000 full knowledge rows solely to compute a count and separately queries latest health-check and idea-grade records already present as the first rows of newest-first histories; the rendered page does not use the other dashboard insight lists, reports, or statistics. Issue `#180` targets a page-specific projection that reduces the handler to 2 database operations without changing the separate Suggestions consumer of the full dashboard. Acceptance should use a populated production-shaped handler benchmark, query-count instrumentation, latency/allocation measurements, and preservation of rendered health-check and idea-grade behavior.
- The runtime notification inbox `list_alerts` discovery path selects all 25 alert columns and JSON-decodes metadata for up to 100 rows before the selected item is fetched through `get_alert`. Because alert bodies support 20,000 characters, one maximum page materializes and reserializes at least 2,000,000 body bytes, plus metadata whose storage size is unbounded. Issue `#193` targets a compact summary projection with zero body materialization, at least 90% lower response bytes and allocations, and production-shaped end-to-end latency/contention benchmarks while preserving inbox discovery, filtering, ordering, pagination, and detail retrieval semantics.
- The mounted `GET /workflows` discovery response combines every workflow's full configuration, every template's full definition, and every model's full configuration. With 50 model configurations containing 1 MiB request bodies, the endpoint must read and serialize at least 50 MiB before accounting for workflow and template data, while existing coverage only checks that a response key exists. Issue `#199` targets compact discovery projections and a complete-handler benchmark, with acceptance requiring at least 90% lower response bytes and allocations while preserving discovery behavior.
- Production scheduler polling has a distinct pre-dispatch query fan-out beyond the known due-schedule task lookup seam: because production always installs `AutomationRepo`, each due schedule first performs an indexed Automation ownership lookup, and each ordinary non-Automation schedule then performs a second full 30-column task query. A poll with `N` due schedules and `U` ordinary schedules therefore performs `1 + N + U` serialized reads on the single-connection SQLite pool before status/category writes and dispatch; 100 ordinary due schedules imply 201 reads. Pending performance notification `40b9d1d0f5fa5a4ac60d8485e9c3c7d5` targets this distinct ownership/type-resolution overhead, with query-count instrumentation and a production-shaped benchmark requiring at least 50% lower median latency and allocations while preserving schedule routing and dispatch behavior.
- Performance fixes must preserve existing ordering, authorization, cancellation, retry, lifecycle, persistence, and projection semantics. Validate with representative fixtures and query plans rather than relying only on microbenchmarks.
- Durable cost centers are handler/service tests, streaming parser and chunk-reassembly coverage, repeated test database/context setup, and DB/git/worktree-heavy service tests.
- The GitHub Actions test workflow caches downloaded Linux desktop apt archives rather than installed system state. Its versioned cache key includes runner OS/image version, architecture, and a digest of the exact GTK/WebKit package manifest; `apt-get update` and the complete install remain unconditional so empty, partial, or invalidated caches retain a working cold path. Workflow action references are pinned to full commit SHAs.

Validation conventions:
- Prefer `make test`, `make test-cover`, or `go test ./... -count=1 -timeout 120s` for authoritative full validation. Raw 60-second full-suite runs can time out under load.
- Use readiness polling instead of fixed sleeps. Timing-sensitive tests may use `testing.Short()` guards.
- Full-suite failures can be environmental, including desktop/config `PATH`, macOS Wails linker warnings, SQLite locks, date-sensitive scheduling tests, and plugin marketplace network timeouts. Distinguish repeatable touched-scope regressions from environmental failures; `TMPDIR=/private/tmp` is the established fallback when default temp-directory plugin cloning hangs.
- Browser performance fixtures should run over localhost HTTP rather than `file://`, and harnesses should expose an explicit DOM pass/fail marker because headless Chrome may leave its parent process alive.
- Real-browser coverage is required for HTMX history/restoration, streaming offsets, transcript scrolling/hydration, drafts, pending attachments, and stale-response races where DOM timing is part of correctness. Canonical rendering behavior lives in `realtime_and_frontend_patterns.md`.
- Shared external HTTP retry tests inject clock/backoff hooks so retry behavior is tested without real sleeps; provider-specific tests cover classification, replay safety, and metadata preservation.
- Handler `NewTestContext` enables local repo paths by default. Tests requiring them disabled must explicitly call `SetLocalRepoPathEnabled(false)`.
- Chat mode selector tests use `chat-select-change` custom events with `e.detail.value`, not native select `change` semantics.
- Malformed-JSON logs in service error-path tests are expected unless an assertion or package also fails.
- Concurrency tests should assert final/current observable state rather than requiring notification delivery to precede operation return.
